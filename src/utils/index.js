import { saveStorage, getStorage } from "./storage";
import {
  formatDateTime,
  formatTimeDate,
  formatDate,
  formatDateFromMili,
  getTime,
} from "./format";

export {
  saveStorage,
  getStorage,
  formatDateTime,
  formatTimeDate,
  formatDate,
  formatDateFromMili,
  getTime,
};
